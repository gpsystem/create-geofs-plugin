// reviewers, is there a better way of doing this?
// @ts-ignore assigning to global is allowed, we need to use declare const when referencing this though
global.GLOBAL_TEST_DIRECTORIES = [] as number[];
