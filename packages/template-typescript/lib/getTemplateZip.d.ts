/// <reference types="node" />
/**
 * Gets the template.
 * @returns null if there is no template, otherwise, returns a Buffer of the template.zip
 */
export declare function getTemplateZip(): Buffer | null;
